!!!
%html
  %head
    %title= "Backbone :: #{yield(:title)}"
    = csrf_meta_tag
    = javascript_include_tag :defaults
    = render '/layouts/fb_js'
    = yield :js
  
    = stylesheet_link_tag 'compiled/modal.css', :media => 'screen, projection'
    = stylesheet_link_tag 'compiled/print.css', :media => 'print'
    /[if IE]
      = stylesheet_link_tag 'compiled/ie.css', :media => 'screen, projection'
    = yield :css
  %body.bp.two-col
    #fb-root
    #flash
      = render '/layouts/flash', :flash => flash
    #container
      #header
        .panel
      #content
        .panel
          %h1= yield :title
          = yield
      #sidebar
        .panel
          = yield :sidebar
      #footer
        .panel
          